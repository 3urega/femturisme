[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not integration"
markers =
    integration: tests that require MySQL staging (MYSQL_* env vars)
    network: tests that call external HTTP or scraping (reserved)
